import 'dart:ui';

abstract class AppColors {
  static const Color primary = Color(
    0xFF149954,
  ); // Darker green for buttons & accents
  static const Color background = Color(
    0xFFF8F9FA,
  ); // Light grayish-white for background
  static const Color card = Color(0xFFFFFFFF);
  // White for cards & containers
  static const Color textPrimary = Color(
    0xFF24252C,
  ); // Black/dark gray for main text
  static const Color textSecondary = Color(
    0xFF6E6A7C,
  ); // Gray for secondary text

  static const Color whiteText = Color(0xFFFFFFFF);
  static const Color success = Color(0xFF4BB543);
  static const Color error = Color(0xFFD8000C);
}
